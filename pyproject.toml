[tool.ruff.lint]
# Правила, на которых фокусируемся: F (критичные), SIM (упрощение), C4 (comprehensions), B (bugbear)
select = ["F", "SIM", "C4", "B"]
# Исключаем правила, противоречащие стилю проекта
extend-ignore = [
    "E501",     # line-too-long: длинные строки (часто в сообщениях/форматировании)
    "BLE001",   # blind-except: ловля широких исключений (принято для устойчивости)
    "SIM117",   # multiple-with-statements: вложенные with
    "LOG015",   # root-logger-call: использование корневого логгера
    "RUF013",   # implicit-optional: неявный Optional (стиль проекта)
    "RUF059",   # unused-unpacked-variable: в тестах
    "B017",     # assert-raises-exception: общие исключения в тестах
    "C401",     # unnecessary-generator-set
    "ASYNC230", # blocking-open-call-in-async-function
    "LOG014",   # exc-info-outside-except-handler
    "S110",     # try-except-pass
    "TRY002",   # raise-vanilla-class
    "B028",     # no-explicit-stacklevel: не требуется
    "B904",     # raise-without-from-inside-except: обработка ошибок без raise from
    "SIM108",   # use-ternary-operator: тернарники не всегда читаемее
    "SIM102",   # collapsible-if: не всегда улучшает читаемость
    "SIM103",   # needless-bool: не всегда улучшает читаемость
    "SIM401",   # dict-get-with-default: sqlite3.Row не поддерживает .get()
]